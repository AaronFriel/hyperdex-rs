# Root Ledger

This is the active root ledger for the current phase of `hyperdex-rs`.

Earlier orchestration history is archived under
[archive/phase-1/ledger.md](docs/archive/phase-1/ledger.md).

| Iteration | Timestamp (UTC) | Action | Evidence | Disposition | Next root move |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-28 10:20Z | Reset the root documentation layout for the post-baseline phase. | The old root package moved to `docs/archive/phase-1`; the active root files are now `docs/autoplan.md` and `docs/ledger.md`; workstreams are split into `active`, `backlog`, and `completed`; and `docs/workstreams.md` becomes the tracked index. | `advance` | Update instructions and gitignore for local worktree tracking, then verify the reorganized docs tree. |
| 2 | 2026-03-28 10:32Z | Added the capability grouping note and reshaped the backlog around dependency order instead of a flat feature list. | `docs/capability-ladder.md` now groups the roadmap into foundation, distributed semantics, programmability, data models/query layers, and storage products; `docs/autoplan.md`, `docs/workstreams.md`, and `docs/future-directions.md` now reference that structure; and backlog workstreams now include programmability, graph/vector, temporal, and object-storage tracks. | `advance` | Keep the active board small, and only promote backlog workstreams when their dependencies and validators are ready. |
| 3 | 2026-03-28 10:40Z | Promoted panic hardening into the active priority set alongside async cleanup. | `rg` on the current tree shows widespread `unwrap` and `expect` usage in runtime, protocol, and test surfaces; the active root plan now includes a dedicated panic-hardening workstream; and the architecture notes now call out no-panic contracts and gradual Clippy ratchets as foundation work. | `advance` | Define the first bounded panic-hardening pass over entry points and public runtime surfaces, then land it with a focused validator. |
| 4 | 2026-03-28 18:15Z | Realigned the root package for four active parallel workstreams and the updated worker model. | The repository now has four dedicated worktrees for the active workstreams; the root AutoPlan explicitly names all four active forks; `validation-ci` is now required to validate workflows locally with `act`; and each active workstream ledger is preregistered with its real worktree path, owned scope, and fast validator. | `advance` | Arm the watchdog, launch the four owned forks, and reconcile the first substantive code result instead of doing more root-only maintenance. |
| 5 | 2026-03-28 23:35Z | Reconciled the first two substantive fork results onto `main`. | `ef0879f` removed `async_trait` from the admin/client protocol service boundary; the failure-testing workstream added a new deterministic schema-convergence proof plus a server-side distributed-read fix; and root validation confirmed the new simulation test passes locally. | `advance` | Keep validation-ci and panic-hardening active, bank the failure-testing commit, and relaunch the next async pass only after the remaining active workstreams either land or need help. |
| 6 | 2026-03-28 23:55Z | Finished the four-way parallel round and reconciled the product results onto `main`. | `main` now contains `54c406f` for act-backed CI workflows, `5777625` plus `c2b2c10` and `ef0879f` for the async cleanup sequence, `adc5b25`, `f4e4215`, and `4a3e876` for adversarial replication and schema-convergence fixes, and `694545e` plus `44f8c58` for two decoder hardening passes. Root validation also confirmed `actionlint`, `act --list`, `act -W .github/workflows/acceptance.yml -j quick-live-acceptance --pull=false`, `cargo test -p legacy-protocol`, `cargo test -p hyperdex-admin-protocol`, and `cargo test -p simulation-harness` for the new targeted proofs. | `advance` | Move the finished CI and async workstreams out of the active board, keep failure-testing and panic-hardening active, and pick the next promotion from the backlog after the workspace run finishes. |
| 7 | 2026-03-29 00:20Z | Reopened async-modernization after verifying remaining tonic async-trait usage. | `rg -n "\\#\\[tonic::async_trait\\]|\\#\\[async_trait\\]|async_trait" crates/transport-grpc crates/server/src/main.rs` still reports `#[tonic::async_trait]` in `crates/server/src/main.rs` and `crates/transport-grpc/src/lib.rs`; the filesystem now places `async-modernization` back under `docs/workstreams/active/`; and the root AutoPlan package has been corrected to treat async cleanup as active again instead of completed. | `advance` | Rearm the watchdog, preregister the next tonic-focused async pass, and launch async-modernization, failure-testing, and panic-hardening in parallel with concrete validators. |
| 8 | 2026-03-29 08:15Z | Recreated the `database-corpus` planning package in the backlog instead of the active board. | `./tmp/hyperdex-db-corpus` still contains the local comparison artifacts; the root AutoPlan now lists `database-corpus` only under backlog; and the workstream package lives under `docs/workstreams/backlog/database-corpus/`. | `advance` | Leave the corpus dormant unless it becomes necessary for a concrete product investigation, and keep active attention on product workstreams. |
| 9 | 2026-03-29 01:00Z | Reconciled the resumed failure-testing and panic-hardening round, and sharpened async-modernization with tonic-generated evidence. | `main` now contains `7f02478` for a deterministic `ConditionalPut` replica-loss proof and `dd00c13` plus `5434a7b` for checked legacy-frontend identify decoding and its regression tests. The active async workstream also now records the real remaining constraint: tonic-build 0.12.3 still generates `#[async_trait]` server traits in `target/debug/build/*/out/hyperdex.v1.rs`, which is why `crates/transport-grpc/src/lib.rs` and `crates/server/src/main.rs` still carry `#[tonic::async_trait]`. | `advance` | Close the finished workers, keep the watchdog armed, and launch the next async, failure-testing, and panic-hardening passes against the updated targets. |
| 10 | 2026-03-29 18:05Z | Finished the current async, failure-testing, and panic-hardening round with merged-state validation and one root-side regression repair. | `main` now contains `11275fd`, `69020d9`, `b6ae810`, `a4ea7d3`, `fb77107`, `f9f76af`, `20c6d71`, and the root follow-up `2b7d144`. Source-controlled `async-trait` usage is gone, the distributed runtime has stronger rollback and schema-gap behavior, oversized BusyBee frames are rejected before payload allocation, and `cargo test --workspace` is green again after tightening delete-group snapshot skip semantics. | `advance` | Move async-modernization to completed, promote fuzzing-hardening into the active board, and launch the next three product-only workstreams from the updated priorities. |
| 11 | 2026-03-29 18:25Z | Added a dedicated fast-feedback nextest track to the active board. | The active priorities now explicitly include a `cargo nextest` migration with `.config/nextest.toml`, a 30-second fast suite target, slower-test naming rules, and `.agent/check.sh` integration. This keeps the next round focused on shortening feedback loops instead of only adding more coverage. | `advance` | Preregister the nextest workstream and launch it alongside failure-testing, panic-hardening, and fuzzing. |
| 12 | 2026-03-29 19:05Z | Reconciled the next tooling round onto `main` and verified the merged branch. | `main` now contains `7abf7fc` for the protocol-clippy ratchet, `b719b65` for the initial decoder fuzz harnesses and a real legacy-protocol allocation hardening fix, and `e76e696` for the nextest fast-feedback path. `cargo nextest run --workspace -E 'not test(/^slow_/)'`, `.agent/check.sh`, and `cargo test --workspace` all pass on the merged branch. | `advance` | Record the merged nextest and fuzz outcomes in their workstream ledgers, record the parked `3ad0c32` failure-testing result, then rearm the watchdog against the corrected active set. |
| 13 | 2026-03-29 19:20Z | Reframed the active board around simulation value after the fast-feedback and first-pass hardening work landed. | `nextest-fast-feedback`, `fuzzing-hardening`, and the current `panic-hardening` pass are done enough to move out of the active board for now. The new active set keeps `failure-testing` and adds `simulation-applicability` plus `recovery-ordering` so the next round is explicitly about getting more proof value from Turmoil, Madsim, and Hegel. | `advance` | Move those finished workstreams to completed, add the new simulation-focused workstreams, and rearm the watchdog on the updated active set. |
| 14 | 2026-03-29 19:25Z | Finished the active-board reshuffle and recorded the parked convergence result. | The filesystem now matches the active board: `panic-hardening`, `fuzzing-hardening`, and `nextest-fast-feedback` live under `completed`, while `failure-testing`, `simulation-applicability`, and `recovery-ordering` are the active set. The fuzzing and nextest ledgers now record their merged outcomes, and `failure-testing` records the parked `3ad0c32` ownership-convergence result. | `advance` | Rearm the watchdog on the new active set and start the next simulation-focused passes. |
| 15 | 2026-03-29 20:25Z | Reconciled the first split proof round onto `main`. | `main` now has a dedicated Hegel property for distributed delete-group/search/count semantics, a stricter local-primary ownership confirmation path in `transport-core` and `server`, and three new deterministic recovery/ownership proofs in `simulation-harness`. `cargo test -p simulation-harness` and `cargo test -p server` both passed on the merged tree. The active board is also now explicit about the tool split: `distributed-simulation` for Turmoil/Madsim and `hegel-properties` for generated property testing. | `advance` | Keep those two proof tracks active and choose the next ownership-convergence or recovery scenario that is not already covered by the new write-path checks. |
| 16 | 2026-03-29 21:05Z | Brought the control files back in sync with the next proof round already landed on `main`. | Since iteration 15, `main` added `87e785d` for a mixed-operation Hegel runtime model, `e3900b7` for a second stale-rejoin recovery proof, `1f38c03` for Hegel placement invariants in `placement-core`, and `750809f` for a real stale-primary delete recovery fix plus its deterministic proof. Root validation confirmed `cargo test -p simulation-harness`, `cargo test -p placement-core hegel_placement_strategies_preserve_replica_invariants_and_input_order_independence -- --nocapture`, and `cargo test -p server` on the merged tree. | `advance` | Keep the active board stable, avoid the precommit-hook surface another agent owns, and choose the next Hegel target outside `simulation-harness` plus the next ownership-convergence mutation scenario beyond `Put` and `Delete`. |
| 17 | 2026-03-28 19:18Z | Promoted a new `history-scrub` workstream to remove `/home/friel` references and make the cleanup repeatable as a history rewrite. | `AGENTS.md` now requires repository-root-relative paths for new repository-authored paths, `docs/autoplan.md` and `docs/workstreams.md` now list `history-scrub` as active, and `docs/workstreams/active/history-scrub/` now contains a plan and preregistered ledger entry for the first owned pass. | `advance` | Create the dedicated worktree, launch the first owned implementation pass on the scan/rewrite toolchain, and reconcile the result with a before/after count. |
