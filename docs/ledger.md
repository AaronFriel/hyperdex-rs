# Root Ledger

This is the active root ledger for the current phase of `hyperdex-rs`.

Earlier orchestration history is archived under
[archive/phase-1/ledger.md](/home/friel/c/aaronfriel/hyperdex-rs/docs/archive/phase-1/ledger.md).

| Iteration | Timestamp (UTC) | Action | Evidence | Disposition | Next root move |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-28 10:20Z | Reset the root documentation layout for the post-baseline phase. | The old root package moved to `docs/archive/phase-1`; the active root files are now `docs/autoplan.md` and `docs/ledger.md`; workstreams are split into `active`, `backlog`, and `completed`; and `docs/workstreams.md` becomes the tracked index. | `advance` | Update instructions and gitignore for local worktree tracking, then verify the reorganized docs tree. |
| 2 | 2026-03-28 10:32Z | Added the capability grouping note and reshaped the backlog around dependency order instead of a flat feature list. | `docs/capability-ladder.md` now groups the roadmap into foundation, distributed semantics, programmability, data models/query layers, and storage products; `docs/autoplan.md`, `docs/workstreams.md`, and `docs/future-directions.md` now reference that structure; and backlog workstreams now include programmability, graph/vector, temporal, and object-storage tracks. | `advance` | Keep the active board small, and only promote backlog workstreams when their dependencies and validators are ready. |
| 3 | 2026-03-28 10:40Z | Promoted panic hardening into the active priority set alongside async cleanup. | `rg` on the current tree shows widespread `unwrap` and `expect` usage in runtime, protocol, and test surfaces; the active root plan now includes a dedicated panic-hardening workstream; and the architecture notes now call out no-panic contracts and gradual Clippy ratchets as foundation work. | `advance` | Define the first bounded panic-hardening pass over entry points and public runtime surfaces, then land it with a focused validator. |
| 4 | 2026-03-28 18:15Z | Realigned the root package for four active parallel workstreams and the updated worker model. | The repository now has four dedicated worktrees for the active workstreams; the root AutoPlan explicitly names all four active forks; `validation-ci` is now required to validate workflows locally with `act`; and each active workstream ledger is preregistered with its real worktree path, owned scope, and fast validator. | `advance` | Arm the watchdog, launch the four owned forks, and reconcile the first substantive code result instead of doing more root-only maintenance. |
| 5 | 2026-03-28 23:35Z | Reconciled the first two substantive fork results onto `main`. | `ef0879f` removed `async_trait` from the admin/client protocol service boundary; the failure-testing workstream added a new deterministic schema-convergence proof plus a server-side distributed-read fix; and root validation confirmed the new simulation test passes locally. | `advance` | Keep validation-ci and panic-hardening active, bank the failure-testing commit, and relaunch the next async pass only after the remaining active workstreams either land or need help. |
| 6 | 2026-03-28 23:55Z | Finished the four-way parallel round and reconciled the product results onto `main`. | `main` now contains `54c406f` for act-backed CI workflows, `5777625` plus `c2b2c10` and `ef0879f` for the async cleanup sequence, `adc5b25`, `f4e4215`, and `4a3e876` for adversarial replication and schema-convergence fixes, and `694545e` plus `44f8c58` for two decoder hardening passes. Root validation also confirmed `actionlint`, `act --list`, `act -W .github/workflows/acceptance.yml -j quick-live-acceptance --pull=false`, `cargo test -p legacy-protocol`, `cargo test -p hyperdex-admin-protocol`, and `cargo test -p simulation-harness` for the new targeted proofs. | `advance` | Move the finished CI and async workstreams out of the active board, keep failure-testing and panic-hardening active, and pick the next promotion from the backlog after the workspace run finishes. |
| 7 | 2026-03-29 00:20Z | Reopened async-modernization after verifying remaining tonic async-trait usage. | `rg -n "\\#\\[tonic::async_trait\\]|\\#\\[async_trait\\]|async_trait" crates/transport-grpc crates/server/src/main.rs` still reports `#[tonic::async_trait]` in `crates/server/src/main.rs` and `crates/transport-grpc/src/lib.rs`; the filesystem now places `async-modernization` back under `docs/workstreams/active/`; and the root AutoPlan package has been corrected to treat async cleanup as active again instead of completed. | `advance` | Rearm the watchdog, preregister the next tonic-focused async pass, and launch async-modernization, failure-testing, and panic-hardening in parallel with concrete validators. |
| 8 | 2026-03-29 01:00Z | Reconciled the resumed failure-testing and panic-hardening round, and sharpened async-modernization with tonic-generated evidence. | `main` now contains `7f02478` for a deterministic `ConditionalPut` replica-loss proof and `dd00c13` plus `5434a7b` for checked legacy-frontend identify decoding and its regression tests. The active async workstream also now records the real remaining constraint: tonic-build 0.12.3 still generates `#[async_trait]` server traits in `target/debug/build/*/out/hyperdex.v1.rs`, which is why `crates/transport-grpc/src/lib.rs` and `crates/server/src/main.rs` still carry `#[tonic::async_trait]`. | `advance` | Close the finished workers, keep the watchdog armed, and launch the next async, failure-testing, and panic-hardening passes against the updated targets. |
